# Snippet para incluir no bloco server do Nginx
# Necessário para o Certbot validar o domínio (desafio HTTP-01)

location ^~ /.well-known/acme-challenge/ {
    root /var/www/html;
    default_type "text/plain";
    allow all;
}
